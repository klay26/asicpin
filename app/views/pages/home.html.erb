<div class="jumbotron center">
	<h1>Welcome to Klay's app</h1>
	<% if user_signed_in? %>
	Congratz you are now logged in to transform business
	<% else %>
	<p>
		<%= link_to "Sign up", new_user_registration_path, class: "btn btn-default btn-lg buttonw" %>
		<%= link_to "Log in", new_user_session_path, class: "btn btn-primary btn-lg buttonw" %>
  	</p>
  	<% end %>
</div>